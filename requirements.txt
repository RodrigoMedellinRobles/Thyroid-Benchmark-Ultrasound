# ThyroidBench — pinned Python dependencies
#
# Python 3.11+ required (numpy 2.3 / pandas 3.0 dropped 3.10). The reported
# results were produced on Python 3.12 with CUDA 12.8 on a single RTX PRO 6000,
# and the versions below are that environment.
#
# PyTorch is pinned to a CUDA 12.8 build. If your CUDA/driver differs, install
# the matching torch/torchvision from https://pytorch.org first, then `pip install -r`
# the rest. Replace the "+cu128" wheels accordingly.

# ── Deep learning core ──────────────────────────────────────────────
torch==2.7.0
torchvision==0.22.0
torchmetrics==1.9.0
timm==1.0.22
segmentation-models-pytorch==0.5.0

# ── Foundation-model adaptation ─────────────────────────────────────
peft==0.19.1
accelerate==1.13.0     # pulled in by peft
transformers==5.6.1    # pulled in by peft
# SAM, SAM2 and MedSAM-2 are NOT on PyPI — install them from their upstream
# repositories, pinned to the exact revisions used for the reported results.
# Notebook 00_setup/00_get_open_datasets.ipynb runs these installs and downloads
# the matching checkpoints; each wrapper in thyroidbench/models/ documents the
# exact checkpoint file it loads.
#
#   # SAM (ViT-H) — also provides the encoder registry MedSAM loads
#   pip install "git+https://github.com/facebookresearch/segment-anything.git"
#
#   # SAM2
#   pip install "git+https://github.com/facebookresearch/sam2.git@2b90b9f5ceec907a1c18123530e92e794ad901a4"
#
#   # MedSAM-2 — a fork of sam2; clone it into pretrained_models/medsam2_fork/,
#   # which is the path medsam2_wrapper.py puts on sys.path ahead of the FB sam2
#   # package (the fork fixes a feature-size mismatch at 512 px).
#   git clone https://github.com/bowang-lab/MedSAM2.git pretrained_models/medsam2_fork
#   git -C pretrained_models/medsam2_fork checkout 332f30d
#   pip install -e pretrained_models/medsam2_fork
#
# MedSAM needs no package of its own: medsam_wrapper.py builds the model through
# the segment_anything registry and loads the medsam_vit_b.pth checkpoint.
#
# SAM3 is the one model published on PyPI, but its weights are GATED and must be
# requested from its authors before the wrapper can run.
sam3==0.1.3

# nnU-Net v2 is driven as an external CLI (plan / train / predict); the exact
# command sequence is in exp1_fullsupervised/traditional_boxcond/run.ipynb.
nnunetv2==2.7.0

# ── Data / IO ───────────────────────────────────────────────────────
opencv-python==4.13.0.92
pillow==12.2.0         # PIL.Image — core image IO (datasets, model wrappers, preprocessing)
h5py==3.16.0           # ThyroidXL + Stanford AIMI HDF5
numpy==2.3.5
pandas==3.0.2
tqdm==4.67.3
requests==2.32.3        # dataset downloaders (DDTI, TN3K)
rarfile==4.2          # DDTI ships as .rar
gdown>=5.1.0            # TN3K download from Google Drive (handles large-file confirm token)

# ── Augmentation (MIT — never AlbumentationsX/AGPL) ─────────────────
albumentations==1.4.0

# ── Statistics & figures ────────────────────────────────────────────
scipy==1.17.1           # Wilcoxon signed-rank
statsmodels==0.14.6     # Holm-Bonferroni
matplotlib==3.10.8

# ── Experiment tracking ─────────────────────────────────────────────
wandb==0.26.0

# ── Optional (hyperparameter config used by some foundation wrappers) ─
hydra-core==1.3.2
